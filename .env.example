# API Key para Gemini (histórias personalizadas com IA)
# Sem esta chave, o app usa modelos locais predefinidos
GEMINI_API_KEY=

# Banco de dados SQLite (obrigatório para login e Minhas Escolhas)
# Copie para .env: cp .env.example .env
DATABASE_URL="file:./dev.db"

# NextAuth (obrigatório para autenticação)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET=
